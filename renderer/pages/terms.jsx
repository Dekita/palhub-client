
import MarkdownPageWrapper from '@components/markdown/wrapper';

export default function TermsOfServicePage({ modals }) {
    return <MarkdownPageWrapper {...{ tagline: "Terms of Service", filename: 'terms' }} />;
}
